# API Backend URL
# For development, the app uses the proxy configured in nuxt.config.ts
# which points to https://127.0.0.1:8081
# For production, set this to your backend API URL
API_BASE_URL=/api-proxy
